- name: create directory 
  become: true
  file: 
    path: /home/ubuntu/web
    state: directory
    mode: 0755
- name: "install pm2"
  become: true
  npm:
    name: pm2
    global: yes
    production: yes
    state: present    
- name: copy artifact
  become: true
  copy: 
    src: ~/project/backend.tar.gz
    dest: /home/ubuntu/web/backend.tar.gz    
- name: Extract artifact files
  become: true
  shell: |
    cd /home/ubuntu/web
    tar -xvf backend.tar.gz
- name: Executing node
  become: true
  shell: |
    cd /home/ubuntu/web
    pm2 stop default
    pm2 start npm -- start
